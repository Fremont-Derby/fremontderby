name: Fremont Derby CodeQL
paths-ignore:
  - test/**
  - scripts/**
  - node_modules/**
queries:
  # Valid suite name for codeql-action ("security" alone is not a valid pack).
  - uses: security-extended
