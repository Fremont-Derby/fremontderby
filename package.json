{
  "name": "fremontderby",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "check": "node --check src/index.js && node --check domain/schedule.js && node --check domain/match.js",
    "dev": "npx wrangler dev",
    "deploy": "npx wrangler deploy"
  }
}
