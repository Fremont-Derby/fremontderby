export const sharedBlindLineupStyles = `
  .layout{display:grid;grid-template-columns:minmax(300px,.9fr) minmax(0,1.1fr);gap:14px}.panel{border:1px solid var(--line);border-radius:12px;background:var(--panel);overflow:hidden}.panel-head{min-height:52px;display:flex;justify-content:space-between;align-items:center;gap:10px;padding:0 12px;border-bottom:1px solid var(--line);font-weight:950}.badge{display:inline-flex;align-items:center;min-height:28px;padding:0 9px;border-radius:999px;background:#26303a;color:#d8e4ea;font-size:.76rem;font-weight:900}.badge.available,.badge.paid{background:rgba(47,169,114,.18);color:#9ee5bd}.badge.unavailable,.badge.blocked{background:rgba(212,91,80,.18);color:#ffb1aa}.badge.unsure{background:rgba(216,173,63,.18);color:#f0d48b}.picker{display:grid;gap:10px;padding:12px}.mobile-lineup-summary{display:none}.candidate-tabs{display:grid;grid-template-columns:1fr 1fr;gap:8px}.candidate-tab{background:transparent;border-color:var(--line);color:#f5f1e9}.candidate-tab[aria-selected="true"]{background:var(--gold);color:#12100a}.section-title{display:flex;justify-content:space-between;align-items:center;color:#dbe4df;font-size:.82rem;font-weight:950;text-transform:uppercase;letter-spacing:.04em}.player-list{display:grid;gap:8px}.player{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;align-items:center;padding:11px;border:1px solid var(--line);border-radius:10px;background:#12171b}.player[data-blocked="true"]{opacity:.62}.player-name{font-weight:950}.meta{display:flex;flex-wrap:wrap;gap:6px;margin-top:5px;color:var(--muted);font-size:.78rem}.add{background:var(--green);color:#06120d;padding:0 13px}.empty{padding:14px;color:var(--muted)}.slots{display:grid;gap:10px;padding:12px}.slot{display:grid;grid-template-columns:44px minmax(0,1fr) auto;gap:9px;align-items:center;min-height:72px;padding:10px;border:1px solid var(--line);border-radius:10px;background:#12171b}.slot-number{width:38px;height:38px;border-radius:9px;display:grid;place-items:center;background:#222932;color:var(--gold);font-weight:950}.slot-main strong{display:block}.slot-main span{display:block;margin-top:3px;color:var(--muted);font-size:.8rem}.slot-actions{display:flex;gap:6px}.slot-actions button{min-width:44px;min-height:44px;padding:0 9px;background:transparent;border-color:var(--line);color:#f5f1e9}.slot-actions button:focus-visible{outline:3px solid #9ee5bd;outline-offset:2px}.slot-actions .remove{color:#ffb1aa}.forfeit{width:100%;background:transparent;border-color:var(--line);color:#f0d48b}.lock-note{padding:0 12px 12px;color:#9ee5bd;font-size:.88rem}.actions{display:grid;grid-template-columns:1fr 1fr;gap:10px;padding:0 12px 12px}.submit,.mobile-lineup-lock{background:var(--green);color:#06120d}.refresh{background:transparent;border-color:var(--line);color:#f5f1e9}.opponent{margin:0 12px 12px;padding:10px;border:1px solid var(--line);border-radius:10px;background:#101419}.opponent strong{display:block;margin-bottom:7px}.opponent-row{display:grid;grid-template-columns:36px minmax(0,1fr);gap:8px;padding:5px 0}.score-link{display:inline-flex;margin:0 12px 12px;min-height:44px;align-items:center;padding:0 12px;border-radius:9px;background:var(--gold);color:#17120a;text-decoration:none;font-weight:950}.hint{padding:0 12px 12px;color:var(--muted);font-size:.82rem}
  @media(max-width:800px){.layout,.actions{grid-template-columns:1fr}.picker-panel{overflow:visible}.mobile-lineup-summary{position:sticky;top:70px;z-index:20;display:grid;gap:8px;padding:10px;border:1px solid var(--line);border-radius:11px;background:rgba(18,23,27,.97);box-shadow:0 10px 26px rgba(0,0,0,.35);backdrop-filter:blur(10px)}.mobile-lineup-summary-head{display:flex;align-items:center;justify-content:space-between;gap:10px}.mobile-lineup-summary-head strong{font-size:.86rem}.mobile-lineup-summary-slots{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:6px}.mobile-slot{min-width:0;padding:8px;border:1px solid var(--line);border-radius:8px;background:#0d1013}.mobile-slot strong{display:block;color:var(--gold);font-size:.7rem;text-transform:uppercase}.mobile-slot span{display:block;margin-top:3px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:.78rem;font-weight:850}.mobile-lineup-lock{width:100%}.lineup-panel .slots,.lineup-panel .panel-head,.lineup-panel .hint,.lineup-panel .actions{display:none}.lineup-panel{border:0;background:transparent}.lineup-panel .lock-note,.lineup-panel .opponent,.lineup-panel .score-link{margin-left:0;margin-right:0}.player{grid-template-columns:1fr auto}.slot{grid-template-columns:42px minmax(0,1fr)}.slot-actions{grid-column:2;justify-content:flex-start}}
`;

export const sharedBlindLineupMarkup = `
  <div data-shared-blind-lineup>
    <section class="layout">
      <article class="panel picker-panel">
        <div class="panel-head"><span>Pick your three</span><span class="badge" data-availability-count>0</span></div>
        <div class="picker">
          <section class="mobile-lineup-summary" data-mobile-lineup-summary aria-label="Current lineup">
            <div class="mobile-lineup-summary-head"><strong>Your lineup</strong><span class="badge" data-mobile-slot-count>0 / 3</span></div>
            <div class="mobile-lineup-summary-slots" data-mobile-lineup-slots aria-live="polite"></div>
            <button class="mobile-lineup-lock" data-mobile-submit type="button">Lock lineup</button>
          </section>
          <div class="candidate-tabs" role="tablist" aria-label="Player source">
            <button class="candidate-tab" data-candidate-tab="roster" role="tab" aria-selected="true" type="button">My roster</button>
            <button class="candidate-tab" data-candidate-tab="subs" role="tab" aria-selected="false" type="button">Find a sub</button>
          </div>
          <label class="search-label">Search names<input data-player-search type="search" placeholder="Type a player name" autocomplete="off" /></label>
          <section data-candidate-panel="roster">
            <div class="section-title"><span>My roster</span><span data-roster-count>0</span></div>
            <div class="player-list" data-roster-list></div>
          </section>
          <section data-candidate-panel="subs" hidden>
            <div class="section-title"><span>Paid + available substitutes</span><span data-sub-count>0</span></div>
            <div class="hint">These players marked themselves available for this date and can be added to your lineup.</div>
            <div class="player-list" data-sub-list></div>
          </section>
          <div data-availability-body hidden></div>
        </div>
      </article>
      <article class="panel lineup-panel">
        <div class="panel-head"><span>Your blind-order lineup</span><span class="badge" data-slot-count>0 / 3</span></div>
        <div class="slots" data-slots></div>
        <div class="hint">Choose exactly three slots. A blank slot must be explicitly marked Forfeit. Opponent order stays hidden until both teams submit.</div>
        <div class="lock-note" data-lock-note hidden>Your lineup is locked. Opponent order stays hidden until both teams submit.</div>
        <div class="opponent" data-opponent hidden><strong>Opponent lineup</strong><div data-opponent-body></div></div>
        <div class="actions"><button class="submit" data-submit type="button">Lock lineup</button><button class="refresh" data-refresh type="button">Refresh</button></div>
        <a class="score-link" data-score-link href="/scorecard" hidden>Score the three matches</a>
      </article>
    </section>
  </div>`;

export const sharedBlindLineupControllerSource = `
function createBlindLineupController(adapter){
  const root=document.querySelector('[data-shared-blind-lineup]');const rosterList=root.querySelector('[data-roster-list]');const subList=root.querySelector('[data-sub-list]');const rosterCount=root.querySelector('[data-roster-count]');const subCount=root.querySelector('[data-sub-count]');const availabilityCount=root.querySelector('[data-availability-count]');const searchInput=root.querySelector('[data-player-search]');const tabs=[...root.querySelectorAll('[data-candidate-tab]')];const rosterPanel=root.querySelector('[data-candidate-panel="roster"]');const subPanel=root.querySelector('[data-candidate-panel="subs"]');const slotsEl=root.querySelector('[data-slots]');const slotCount=root.querySelector('[data-slot-count]');const submitButton=root.querySelector('[data-submit]');const mobileSlotsEl=root.querySelector('[data-mobile-lineup-slots]');const mobileSlotCount=root.querySelector('[data-mobile-slot-count]');const mobileSubmitButton=root.querySelector('[data-mobile-submit]');const refreshButton=root.querySelector('[data-refresh]');const lockNote=root.querySelector('[data-lock-note]');const opponentEl=root.querySelector('[data-opponent]');const opponentBody=root.querySelector('[data-opponent-body]');const scoreLink=root.querySelector('[data-score-link]');let availabilityRows=[];let lineupLocked=false;let selectedSlots=[null,null,null];let activeCandidateTab='roster';
  function status(message,tone){if(adapter.setStatus)adapter.setStatus(message,tone)}function isSelected(playerId){return selectedSlots.some((slot)=>slot&&slot.playerId===playerId)}function badge(textValue,className){const span=document.createElement('span');span.className='badge '+(className||'');span.textContent=textValue;return span}
  function setCandidateTab(tab){activeCandidateTab=tab==='subs'?'subs':'roster';for(const button of tabs)button.setAttribute('aria-selected',String(button.dataset.candidateTab===activeCandidateTab));rosterPanel.hidden=activeCandidateTab!=='roster';subPanel.hidden=activeCandidateTab!=='subs';searchInput.placeholder=activeCandidateTab==='subs'?'Search available substitutes':'Search your roster';renderCandidates()}
  function playerCard(row){const card=document.createElement('div');card.className='player';card.dataset.blocked=String(!row.eligible);const info=document.createElement('div');const name=document.createElement('div');name.className='player-name';name.textContent=row.display_name;const meta=document.createElement('div');meta.className='meta';meta.append(badge(row.availability_status||'unsure',row.availability_status||'unsure'),badge(row.fargo_rating==null?'Unrated':'Fargo '+row.fargo_rating),badge((row.regular_matches_scheduled||0)+'/7 played'),badge(row.payment_status||'unpaid',(row.payment_status==='paid'||row.payment_status==='waived')?'paid':'blocked'));if(row.eligibility_reason)meta.append(badge(row.eligibility_reason,'blocked'));info.append(name,meta);const add=document.createElement('button');add.type='button';add.className='add';add.textContent=isSelected(row.player_id)?'Selected':'Add';add.disabled=lineupLocked||!row.eligible||isSelected(row.player_id)||selectedSlots.every(Boolean);add.dataset.addPlayer=row.player_id;card.append(info,add);return card}
  function renderCandidates(){const query=searchInput.value.trim().toLowerCase();const matches=(row)=>!query||String(row.display_name).toLowerCase().includes(query);const roster=availabilityRows.filter((row)=>row.participation_type==='roster'&&matches(row));const subs=availabilityRows.filter((row)=>row.participation_type!=='roster'&&row.eligible&&row.availability_status==='available'&&matches(row));rosterList.replaceChildren();subList.replaceChildren();for(const row of roster)rosterList.append(playerCard(row));for(const row of subs)subList.append(playerCard(row));if(!roster.length){const empty=document.createElement('div');empty.className='empty';empty.textContent=query?'No roster players match “'+searchInput.value.trim()+'”.':'No roster players found.';rosterList.append(empty)}if(!subs.length){const empty=document.createElement('div');empty.className='empty';empty.textContent=query?'No available substitutes match “'+searchInput.value.trim()+'”.':'No paid, eligible players are currently marked available as substitutes.';subList.append(empty)}rosterCount.textContent=String(roster.length);subCount.textContent=String(subs.length);availabilityCount.textContent=String(availabilityRows.length)}
  function findPlayer(playerId){return availabilityRows.find((row)=>row.player_id===playerId)||null}function addPlayer(playerId){if(lineupLocked)return;const row=findPlayer(playerId);if(!row||!row.eligible||isSelected(playerId))return;const index=selectedSlots.findIndex((slot)=>!slot);if(index<0)return;selectedSlots[index]={playerId:row.player_id,name:row.display_name,type:row.participation_type};renderSlots();renderCandidates()}
  function renderMobileSummary(){mobileSlotsEl.replaceChildren();for(let index=0;index<3;index+=1){const slot=selectedSlots[index];const item=document.createElement('div');item.className='mobile-slot';const label=document.createElement('strong');label.textContent='Slot '+(index+1);const value=document.createElement('span');value.textContent=slot?(slot.forfeit?'Forfeit':slot.name):'Empty';item.append(label,value);mobileSlotsEl.append(item)}const filled=selectedSlots.filter(Boolean).length;mobileSlotCount.textContent=filled+' / 3';mobileSubmitButton.disabled=lineupLocked||filled!==3}
  function renderSlots(){slotsEl.replaceChildren();for(let index=0;index<3;index+=1){const slot=selectedSlots[index];const row=document.createElement('div');row.className='slot';const number=document.createElement('div');number.className='slot-number';number.textContent=String(index+1);const main=document.createElement('div');main.className='slot-main';const strong=document.createElement('strong');const detail=document.createElement('span');if(slot){strong.textContent=slot.forfeit?'Forfeit':slot.name;detail.textContent=slot.forfeit?'No individual loss is created.':(slot.type==='roster'?'Roster player':'Substitute')}else{strong.textContent='Empty slot';detail.textContent='Add a player or mark this slot Forfeit.'}main.append(strong,detail);const actions=document.createElement('div');actions.className='slot-actions';if(!lineupLocked){if(slot){if(index>0){const up=document.createElement('button');up.type='button';up.textContent='↑';up.setAttribute('aria-label','Move slot '+(index+1)+' up');up.dataset.moveUp=String(index);actions.append(up)}if(index<2){const down=document.createElement('button');down.type='button';down.textContent='↓';down.setAttribute('aria-label','Move slot '+(index+1)+' down');down.dataset.moveDown=String(index);actions.append(down)}const remove=document.createElement('button');remove.type='button';remove.textContent='Remove';remove.className='remove';remove.dataset.removeSlot=String(index);actions.append(remove)}else{const forfeit=document.createElement('button');forfeit.type='button';forfeit.textContent='Forfeit slot';forfeit.className='forfeit';forfeit.dataset.forfeitSlot=String(index);actions.append(forfeit)}}row.append(number,main,actions);slotsEl.append(row)}const filled=selectedSlots.filter(Boolean).length;slotCount.textContent=filled+' / 3';submitButton.disabled=lineupLocked||filled!==3;renderMobileSummary()}
  function moveSlot(from,to){const tmp=selectedSlots[from];selectedSlots[from]=selectedSlots[to];selectedSlots[to]=tmp;renderSlots()}function removeSlot(index){selectedSlots[index]=null;renderSlots();renderCandidates()}function forfeitSlot(index){selectedSlots[index]={forfeit:true,playerId:'',name:'Forfeit',type:'forfeit'};renderSlots();renderCandidates()}
  function renderAvailability(rows){availabilityRows=[...rows].sort((a,b)=>{const rank={available:0,unsure:1,unavailable:2};return(a.participation_type==='roster'?0:1)-(b.participation_type==='roster'?0:1)||(rank[a.availability_status]??1)-(rank[b.availability_status]??1)||String(a.display_name).localeCompare(String(b.display_name))});renderCandidates();renderSlots()}
  function renderLineups(rows){lineupLocked=rows.some((row)=>row.is_own_team);const ownRows=rows.filter((row)=>row.is_own_team).sort((a,b)=>a.slot_number-b.slot_number);if(lineupLocked){selectedSlots=[0,1,2].map((index)=>{const row=ownRows.find((item)=>item.slot_number===index+1);if(!row||!row.player_id)return{forfeit:true,playerId:'',name:'Forfeit',type:'forfeit'};return{playerId:row.player_id,name:row.display_name||'Player',type:row.participation_type||'roster'}})}const opponentRows=rows.filter((row)=>!row.is_own_team).sort((a,b)=>a.slot_number-b.slot_number);const opponentVisible=opponentRows.length>0;opponentBody.replaceChildren();for(const row of opponentRows){const item=document.createElement('div');item.className='opponent-row';const slot=document.createElement('strong');slot.textContent='#'+row.slot_number;const player=document.createElement('span');player.textContent=row.display_name||(row.player_id?'Player':'Forfeit');item.append(slot,player);opponentBody.append(item)}opponentEl.hidden=!opponentVisible;lockNote.hidden=!lineupLocked;scoreLink.hidden=!(lineupLocked&&opponentVisible);if(adapter.scoreHref)scoreLink.href=adapter.scoreHref;renderSlots();renderCandidates();if(adapter.onLineupState)adapter.onLineupState({lineupLocked,opponentVisible});if(lineupLocked&&opponentVisible)status('Both lineups are locked and revealed. Three matches are ready to score.','ok');else if(lineupLocked)status('Your lineup is locked. Waiting for the opponent captain.','ok')}
  async function loadPage(){status('Loading lineup...');lineupLocked=false;selectedSlots=[null,null,null];const [candidates,lineups]=await Promise.all([adapter.loadCandidates(),adapter.loadLineups()]);renderAvailability(candidates||[]);renderLineups(lineups||[]);if(!lineupLocked)status('Pick three from your roster, or tap Find a sub to see paid players who are available this week.','ok')}
  async function submitLineup(){if(lineupLocked)throw new Error('This lineup is already locked.');if(selectedSlots.filter(Boolean).length!==3)throw new Error('Choose a player or explicit forfeit for all three slots.');const players=selectedSlots.filter((slot)=>slot&&!slot.forfeit).map((slot)=>slot.playerId);if(new Set(players).size!==players.length)throw new Error('Lineup players must be unique');if(!confirm('Lock this lineup? You cannot change the order after submission.'))return;const slots=selectedSlots.map((slot,index)=>({slotNumber:index+1,playerId:slot&&slot.playerId?slot.playerId:''}));status('Locking lineup...');await adapter.submit(slots);await loadPage();if(adapter.afterSubmit)adapter.afterSubmit()}
  async function run(action){try{await action()}catch(error){status(error.message,'error')}}
  searchInput.addEventListener('input',renderCandidates);for(const tab of tabs)tab.addEventListener('click',()=>setCandidateTab(tab.dataset.candidateTab));submitButton.addEventListener('click',()=>run(submitLineup));mobileSubmitButton.addEventListener('click',()=>run(submitLineup));refreshButton.addEventListener('click',()=>run(loadPage));root.addEventListener('click',(event)=>{const button=event.target.closest('button');if(!button)return;if(button.dataset.addPlayer)addPlayer(button.dataset.addPlayer);if(button.dataset.removeSlot!=null)removeSlot(Number(button.dataset.removeSlot));if(button.dataset.forfeitSlot!=null)forfeitSlot(Number(button.dataset.forfeitSlot));if(button.dataset.moveUp!=null)moveSlot(Number(button.dataset.moveUp),Number(button.dataset.moveUp)-1);if(button.dataset.moveDown!=null)moveSlot(Number(button.dataset.moveDown),Number(button.dataset.moveDown)+1)});setCandidateTab('roster');renderSlots();
  return{loadPage:()=>run(loadPage),reset(){lineupLocked=false;selectedSlots=[null,null,null];renderSlots();renderCandidates()},refreshCandidates:()=>run(async()=>renderAvailability(await adapter.loadCandidates()))};
}`;

export function renderBlindLineupComponent() {
  return sharedBlindLineupMarkup;
}
