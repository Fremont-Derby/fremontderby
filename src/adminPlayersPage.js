export function renderAdminPlayersPage() {
  return `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Player Management · Fremont Derby</title>
  <style>
    :root{color-scheme:dark;font-family:Inter,ui-sans-serif,system-ui,sans-serif;background:#0d1110;color:#f3f6f4;--panel:#17201c;--line:#31443a;--muted:#a9b8b0;--green:#45b77c;--gold:#e2bd58;--red:#e36b62}*{box-sizing:border-box}body{margin:0;background:#0d1110}button,input,textarea,select{font:inherit}button,a,input,textarea,select{border-radius:10px}button:focus-visible,a:focus-visible,input:focus-visible,textarea:focus-visible,select:focus-visible{outline:3px solid #9ee5bd;outline-offset:2px}.app{width:min(980px,100%);margin:auto;padding:18px}.head{display:flex;gap:14px;align-items:flex-start;justify-content:space-between;margin-bottom:16px}.head h1{margin:0 0 4px;font-size:clamp(1.7rem,6vw,2.5rem)}.muted{color:var(--muted);line-height:1.45}.back{min-height:44px;display:inline-flex;align-items:center;padding:0 14px;border:1px solid var(--line);color:#dff0e6;text-decoration:none;white-space:nowrap}.panel{border:1px solid var(--line);background:var(--panel);border-radius:14px;padding:14px}.search{display:grid;grid-template-columns:1fr auto;gap:10px}.search input{min-height:48px;padding:0 14px;border:1px solid var(--line);background:#0b100e;color:#fff}.search button,.action{min-height:48px;padding:0 16px;border:1px solid transparent;font-weight:850;cursor:pointer}.search button{background:var(--green);color:#07140d}.status{margin:12px 0 0;min-height:24px;color:var(--muted)}.status[data-tone=error]{color:#ffb5ae}.status[data-tone=ok]{color:#a9e7c0}.list{display:grid;gap:10px;margin-top:14px}.card{border:1px solid var(--line);border-radius:12px;background:#111814;padding:14px;display:grid;gap:12px}.identity{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.identity h2{margin:0;font-size:1.15rem}.badges{display:flex;flex-wrap:wrap;gap:6px}.badge{display:inline-flex;align-items:center;min-height:28px;padding:0 9px;border-radius:999px;background:#26342d;color:#dce8e1;font-size:.76rem;font-weight:850}.badge.admin{background:#4b3b13;color:#ffe597}.badge.blocked{background:#532520;color:#ffc5bf}.badge.ready{background:#183d29;color:#bcebd0}.teams{display:grid;gap:7px}.team{display:flex;align-items:center;justify-content:space-between;gap:10px;padding:7px 9px;border:1px solid var(--line);border-radius:9px;color:#cbd8d1;font-size:.82rem}.team-remove{min-height:38px;padding:0 10px;background:transparent;border:1px solid #7f3f39;color:#ffb5ae;font-weight:800;cursor:pointer}.season-state{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px}.state{padding:9px;border:1px solid var(--line);border-radius:9px}.state span{display:block;color:var(--muted);font-size:.72rem}.state strong{display:block;margin-top:3px;overflow-wrap:anywhere}.control{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:12px;align-items:end;padding-top:10px;border-top:1px solid var(--line)}.control textarea,.control select{width:100%;min-height:48px;padding:10px;border:1px solid var(--line);background:#0b100e;color:#fff}.control textarea{min-height:72px;resize:vertical}.roster-fields{display:grid;grid-template-columns:minmax(0,1fr) minmax(0,1fr);gap:8px}.action.grant,.action.restore,.action.add{background:var(--green);color:#06150d}.action.revoke,.action.block{background:transparent;color:#ffb5ae;border-color:#7f3f39}.action:disabled,.team-remove:disabled{opacity:.45;cursor:not-allowed}.reason{padding:9px 11px;border-left:4px solid var(--red);background:#211513;color:#ffd0cb;border-radius:8px}.empty{padding:22px 8px;text-align:center;color:var(--muted)}
    @media(max-width:640px){.app{padding:12px}.head{display:grid}.back{width:100%;justify-content:center}.search,.control,.season-state,.roster-fields{grid-template-columns:1fr}.identity{display:grid}.action{width:100%}.team{align-items:stretch;display:grid}.team-remove{width:100%}}
  </style>
</head>
<body>
  <main class="app">
    <header class="head"><div><div class="muted">Admin · League Management</div><h1>Players</h1><div class="muted">Manage league-admin access, competition eligibility, and team membership exceptions without IDs or database edits.</div></div><a class="back" href="/admin/operations">Operations</a></header>
    <section class="panel">
      <form class="search" data-search-form><input data-search type="search" autocomplete="off" placeholder="Search player name" aria-label="Search players"/><button type="submit">Find player</button></form>
      <div class="status" role="status" aria-live="polite" data-status>Checking admin access…</div>
      <div class="list" data-list hidden></div><div class="empty" data-empty hidden>No players match that search.</div>
    </section>
  </main>
  <script>
    const listEl=document.querySelector('[data-list]'),emptyEl=document.querySelector('[data-empty]'),statusEl=document.querySelector('[data-status]'),searchEl=document.querySelector('[data-search]');let players=[],rosterTeams=[];
    function token(){return sessionStorage.getItem('fd.accessToken')||''}function setStatus(message,tone=''){statusEl.textContent=message;statusEl.dataset.tone=tone}async function parseJson(response){const text=await response.text();if(!text)return{};try{return JSON.parse(text)}catch{return{error:text}}}async function api(path,options={}){const accessToken=token();if(!accessToken)throw new Error('Sign in from Profile to use league admin tools.');const response=await fetch(path,{...options,headers:{authorization:'Bearer '+accessToken,'content-type':'application/json',...(options.headers||{})}});const body=await parseJson(response);if(!response.ok)throw new Error(body.error||'Request failed');return body}function node(tag,text,className){const el=document.createElement(tag);if(text!=null)el.textContent=text;if(className)el.className=className;return el}function state(label,value){const box=node('div',null,'state');box.append(node('span',label),node('strong',value||'—'));return box}
    async function mutateRoster(player,team,active,reason=''){const verb=active?'add '+player.displayName+' to '+team.teamName:'remove '+player.displayName+' from '+team.teamName;if(!confirm('Confirm: '+verb+'?'))return false;await api('/api/admin/players/'+encodeURIComponent(player.playerId)+'/admin-role',{method:'PUT',body:JSON.stringify({operation:'roster-membership',seasonId:team.seasonId,teamId:team.teamId,active,reason:reason.trim()})});setStatus(player.displayName+(active?' added to ':' removed from ')+team.teamName+'.','ok');await load(false);return true}
    function roleControl(player,card){const box=node('div',null,'control'),label=node('label');label.append(node('span','Admin role note (optional)','muted'));const reason=document.createElement('textarea');reason.maxLength=500;reason.placeholder=player.isLeagueAdmin?'Why remove admin access?':'Why grant admin access?';label.append(reason);const button=node('button',player.isLeagueAdmin?'Revoke admin':'Grant admin','action '+(player.isLeagueAdmin?'revoke':'grant'));button.type='button';button.disabled=!player.hasLogin;button.addEventListener('click',async()=>{const verb=player.isLeagueAdmin?'revoke':'grant';if(!confirm('Confirm: '+verb+' league-admin access for '+player.displayName+'?'))return;button.disabled=true;try{await api('/api/admin/players/'+encodeURIComponent(player.playerId)+'/admin-role',{method:'PUT',body:JSON.stringify({enabled:!player.isLeagueAdmin,reason:reason.value.trim()})});player.isLeagueAdmin=!player.isLeagueAdmin;setStatus(player.displayName+(player.isLeagueAdmin?' now has league-admin access.':' no longer has league-admin access.'),'ok');render()}catch(error){setStatus(error.message,'error');button.disabled=false}});box.append(label,button);card.append(box)}
    function eligibilityControl(player,card){if(!player.currentSeasonId){card.append(node('div','No active season is available for competition eligibility.','muted'));return}const box=node('div',null,'control'),label=node('label');label.append(node('span',player.competitionEligible?'Reason required to mark ineligible':'Reason to restore eligibility (optional)','muted'));const reason=document.createElement('textarea');reason.maxLength=500;reason.placeholder=player.competitionEligible?'Example: suspended pending league review':'Optional note for restoring play';label.append(reason);const button=node('button',player.competitionEligible?'Mark ineligible':'Restore eligibility','action '+(player.competitionEligible?'block':'restore'));button.type='button';button.addEventListener('click',async()=>{const nextEligible=!player.competitionEligible;if(!nextEligible&&!reason.value.trim()){setStatus('Enter a reason before marking '+player.displayName+' ineligible.','error');reason.focus();return}if(!confirm('Confirm: '+(nextEligible?'restore competition eligibility for ':'mark ineligible for competition: ')+player.displayName+' in '+player.currentSeasonName+'?'))return;button.disabled=true;try{const body=await api('/api/admin/players/'+encodeURIComponent(player.playerId)+'/admin-role',{method:'PUT',body:JSON.stringify({operation:'competition-eligibility',seasonId:player.currentSeasonId,eligible:nextEligible,reason:reason.value.trim()})});player.competitionEligible=body.player.competitionEligible;player.ineligibilityReason=body.player.ineligibilityReason;setStatus(player.displayName+(player.competitionEligible?' is eligible to compete again.':' is now blocked from new lineup/scoring activity.'),'ok');render()}catch(error){setStatus(error.message,'error');button.disabled=false}});box.append(label,button);card.append(box)}
    function rosterControl(player,card){if(!player.currentSeasonId)return;const choices=rosterTeams.filter(team=>team.seasonId===player.currentSeasonId&&!player.teams.some(current=>current.seasonId===team.seasonId&&current.teamId===team.teamId));const box=node('div',null,'control'),fields=node('div',null,'roster-fields'),teamLabel=node('label'),noteLabel=node('label');teamLabel.append(node('span','Add to team','muted'));const select=document.createElement('select');select.setAttribute('aria-label','Team to add '+player.displayName+' to');for(const team of choices){const option=document.createElement('option');option.value=team.teamId;option.textContent=team.teamName;select.append(option)}if(!choices.length){const option=document.createElement('option');option.value='';option.textContent='No other active-season teams';select.append(option);select.disabled=true}teamLabel.append(select);noteLabel.append(node('span','Roster exception note (optional)','muted'));const note=document.createElement('textarea');note.maxLength=500;note.placeholder='Why is this admin exception needed?';noteLabel.append(note);fields.append(teamLabel,noteLabel);const button=node('button','Add to team','action add');button.type='button';button.disabled=!choices.length;button.addEventListener('click',async()=>{const team=choices.find(item=>item.teamId===select.value);if(!team)return;button.disabled=true;try{await mutateRoster(player,team,true,note.value)}catch(error){setStatus(error.message,'error');button.disabled=false}});box.append(fields,button);card.append(box)}
    function render(){const q=searchEl.value.trim().toLowerCase(),shown=players.filter(p=>!q||p.displayName.toLowerCase().includes(q));listEl.replaceChildren();emptyEl.hidden=shown.length>0;listEl.hidden=shown.length===0;for(const player of shown){const card=node('article',null,'card'),identity=node('div',null,'identity'),who=node('div');who.append(node('h2',player.displayName));const badges=node('div',null,'badges');badges.append(node('span',player.hasLogin?'Signed in before':'No login yet','badge'));if(player.isLeagueAdmin)badges.append(node('span','League admin','badge admin'));if(player.currentSeasonId)badges.append(node('span',player.competitionEligible?'Competition eligible':'Competition ineligible','badge '+(player.competitionEligible?'ready':'blocked')));identity.append(who,badges);card.append(identity);const teams=node('div',null,'teams');if(player.teams.length){for(const team of player.teams){const row=node('div',null,'team');row.append(node('span',team.teamName+' · '+team.seasonName+' · '+team.role));if(team.membershipId&&team.role==='player'&&team.seasonId===player.currentSeasonId){const remove=node('button','Remove from team','team-remove');remove.type='button';remove.addEventListener('click',async()=>{remove.disabled=true;try{await mutateRoster(player,team,false)}catch(error){setStatus(error.message,'error');remove.disabled=false}});row.append(remove)}teams.append(row)}}else teams.append(node('span','No active team memberships','muted'));card.append(teams);if(player.currentSeasonId){const states=node('div',null,'season-state');states.append(state('Season',player.currentSeasonName),state('Registration',player.registrationStatus||'Not registered'),state('Payment',player.paymentStatus||'Not recorded'));card.append(states)}rosterControl(player,card);if(!player.competitionEligible&&player.ineligibilityReason)card.append(node('div','Ineligible: '+player.ineligibilityReason,'reason'));eligibilityControl(player,card);roleControl(player,card);listEl.append(card)}}
    async function load(showLoading=true){if(showLoading)setStatus('Loading players…');const body=await api('/api/admin/players');players=Array.isArray(body.players)?body.players:[];rosterTeams=Array.isArray(body.rosterTeams)?body.rosterTeams:[];render();if(showLoading)setStatus(players.length+' player'+(players.length===1?'':'s')+' loaded.','ok')}document.querySelector('[data-search-form]').addEventListener('submit',event=>{event.preventDefault();render()});searchEl.addEventListener('input',render);load().catch(error=>{listEl.hidden=true;emptyEl.hidden=true;setStatus(error.message,'error')});
  </script>
</body>
</html>`;
}
