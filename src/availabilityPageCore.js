export function renderAvailabilityPage() {
  return `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Fremont Derby Availability · Check in</title>
  <style>
    :root{font-family:Inter,ui-sans-serif,system-ui,sans-serif;--line:#cfd7d3;--muted:#52635b;--green:#168653;--green-bg:#d9f2e5;--yellow:#9a6500;--yellow-bg:#fff0bd;--red:#b42318;--red-bg:#fde1dc;--neutral:#6b7470;--neutral-bg:#eef1ef}*{box-sizing:border-box}body{margin:0;min-height:100vh}button{font:inherit;border:1px solid transparent;border-radius:8px;font-weight:900;cursor:pointer}button:disabled{cursor:not-allowed;opacity:.58}button:focus-visible,.signin:focus-visible,.retry:focus-visible{outline:3px solid #0c6a46;outline-offset:2px}.app{width:min(760px,100%);margin:0 auto;padding:12px 10px 22px}.intro{display:grid;gap:2px;margin-bottom:8px}.intro h1{margin:0;font-size:clamp(1.55rem,6vw,2rem);letter-spacing:-.03em}.intro p{margin:0;color:var(--muted);line-height:1.3;font-size:.86rem}.status{min-height:18px;margin:4px 0 0;color:var(--muted);font-size:.78rem}.status[data-tone="error"]{color:var(--red)}.status[data-tone="ok"]{color:#0f6b43}.panel{border:1px solid var(--line);border-radius:10px;background:#fff;overflow:hidden}.panel+.panel{margin-top:8px}.panel-head{min-height:42px;display:flex;align-items:center;justify-content:space-between;gap:8px;padding:0 10px;border-bottom:1px solid var(--line);font-weight:900}.badge{display:inline-flex;align-items:center;min-height:26px;border-radius:999px;padding:0 9px;background:#eef5f1;color:#164f38;font-size:.72rem;font-weight:900}.date-list{display:block;border:1px solid var(--line);border-radius:10px;overflow:hidden;background:#fff}.date-card{display:grid;grid-template-columns:minmax(0,1fr) auto 126px;align-items:center;gap:8px;min-height:48px;padding:5px 7px;border-bottom:1px solid rgba(54,70,62,.18);background:#fff;transition:background-color .12s ease}.date-card:last-child{border-bottom:0}.date-card[data-state="available"]{background:var(--green-bg)}.date-card[data-state="unsure"]{background:var(--yellow-bg)}.date-card[data-state="unavailable"]{background:var(--red-bg)}.date-card[data-state="unmarked"]{background:var(--neutral-bg)}.date-copy{min-width:0;display:grid;gap:1px}.date-copy strong{font-size:.92rem;line-height:1.15}.date-copy span{min-width:0;color:#34443d;font-size:.72rem;line-height:1.15;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}.response{display:inline-flex;align-items:center;justify-content:center;min-width:76px;font-size:.72rem;font-weight:900;white-space:nowrap;color:#24352e}.response::before{content:'';width:7px;height:7px;margin-right:5px;border-radius:50%;background:currentColor}.response[data-state="available"]{color:#075f39}.response[data-state="unsure"]{color:#744c00}.response[data-state="unavailable"]{color:#8f1a11}.response[data-state="unmarked"]{color:#525d58}.quick-actions{display:grid;grid-template-columns:repeat(3,1fr);gap:3px}.quick-actions button{min-height:36px;min-width:0;padding:2px 3px;border-width:1px;background:transparent;color:#26372f;font-size:.72rem}.quick-actions button[data-value="available"]{border-color:#63ae8a}.quick-actions button[data-value="unsure"]{border-color:#c99719}.quick-actions button[data-value="unavailable"]{border-color:#d9877f}.quick-actions button[aria-pressed="true"]{box-shadow:inset 0 0 0 2px currentColor;background:transparent}.quick-actions button[aria-pressed="true"]::after{content:' ✓'}.row-status{grid-column:1/-1;min-height:0;margin-top:-2px;color:var(--muted);font-size:.68rem;font-weight:800}.row-status:empty{display:none}.row-status[data-tone="error"]{color:var(--red)}.row-status[data-tone="ok"]{color:#0f6b43}.empty{padding:18px 12px;color:var(--muted);text-align:center;border:1px solid var(--line);border-radius:10px;background:#fff}.recovery{display:grid;gap:8px;padding:14px;border:1px solid var(--line);border-radius:10px;background:#fff}.recovery h1{margin:0;font-size:1.05rem}.recovery p{margin:0;color:var(--muted);line-height:1.35}.recovery-actions{display:flex;flex-wrap:wrap;gap:8px}.signin,.retry{display:inline-flex;align-items:center;justify-content:center;min-height:44px;padding:0 12px;border:1px solid #0d7a4f;border-radius:8px;color:#0b6542;text-decoration:none;font-weight:900}.retry{background:#dff4e9}.choice-list{display:grid;gap:8px;padding:10px}.choice-card{display:grid;gap:8px;padding:10px;border:1px solid var(--line);border-radius:8px;background:#fff}.choice-copy{display:grid;gap:3px;color:var(--muted);font-size:.8rem}.choice-actions{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.choice-team{min-height:44px;background:#fff;border-color:#0d7a4f;color:#0b6542}.choice-team.selected{background:#dff4e9;color:#095d39}.choice-note{color:#805400;font-size:.76rem;font-weight:800}[hidden]{display:none!important}@media(max-width:560px){.app{padding:9px 6px 16px}.intro{margin-bottom:6px}.intro p{font-size:.78rem}.status{font-size:.72rem}.date-card{grid-template-columns:minmax(0,1fr) 70px 112px;gap:4px;min-height:44px;padding:4px 5px}.date-copy strong{font-size:.84rem}.date-copy span{font-size:.65rem}.response{min-width:0;font-size:.65rem}.response::before{width:6px;height:6px;margin-right:3px}.quick-actions{gap:2px}.quick-actions button{min-height:34px;padding:1px;font-size:.67rem}.quick-actions button[aria-pressed="true"]::after{content:''}.choice-actions,.recovery-actions{display:grid;grid-template-columns:1fr}.signin,.retry{width:100%}.panel-head{align-items:flex-start;padding-block:9px}}
  </style>
</head>
<body>
  <main class="app">
    <header class="intro">
      <h1>Check in</h1>
      <p>Mark your upcoming league nights. Green means available, yellow means unsure, red means unavailable.</p>
      <div class="status" data-status role="status" aria-live="polite">Loading your league nights…</div>
    </header>

    <section class="recovery" data-recovery aria-live="polite">
      <h1 data-recovery-title>Loading your league nights…</h1>
      <p data-recovery-copy>Checking your sign-in and upcoming rounds.</p>
      <div class="recovery-actions" data-recovery-actions></div>
    </section>

    <div data-workspace hidden>
      <section class="panel" data-team-choice-panel hidden>
        <div class="panel-head"><span>Choose your team</span><span class="badge">Required before lineups</span></div>
        <div class="choice-list" data-team-choice-list></div>
      </section>

      <section class="date-list" data-date-list role="table" aria-label="Upcoming league nights"></section>
      <div class="empty" data-empty hidden>No published regular-season rounds are available for you yet.</div>
    </div>
  </main>
  <script>
    const statusEl=document.querySelector('[data-status]');const dateList=document.querySelector('[data-date-list]');const emptyEl=document.querySelector('[data-empty]');const teamChoicePanel=document.querySelector('[data-team-choice-panel]');const teamChoiceList=document.querySelector('[data-team-choice-list]');const recovery=document.querySelector('[data-recovery]');const recoveryTitle=document.querySelector('[data-recovery-title]');const recoveryCopy=document.querySelector('[data-recovery-copy]');const recoveryActions=document.querySelector('[data-recovery-actions]');const workspace=document.querySelector('[data-workspace]');let contexts=[];let teamChoices=[];
    function setStatus(message,tone){statusEl.textContent=message;statusEl.dataset.tone=tone||'muted'}function accessToken(){return sessionStorage.getItem('fd.accessToken')||''}async function parseJson(response){const text=await response.text();if(!text)return{};try{return JSON.parse(text)}catch{return{error:text}}}async function signedApi(path,options={}){const token=accessToken();if(!token)throw new Error('Sign in with Google to check in.');const response=await fetch(path,{...options,headers:{authorization:'Bearer '+token,'content-type':'application/json',...(options.headers||{})}});const body=await parseJson(response);if(response.status===401){sessionStorage.removeItem('fd.accessToken');throw new Error('Your sign-in expired. Open Profile and sign in again.')}if(!response.ok)throw new Error(body.error||'Request failed');return body}
    function clearRecoveryActions(){recoveryActions.replaceChildren()}function recoveryLink(label){const link=document.createElement('a');link.className='signin';link.href='/profile';link.textContent=label;return link}function retryButton(){const button=document.createElement('button');button.className='retry';button.type='button';button.textContent='Try again';button.addEventListener('click',()=>run(loadPage));return button}function showRecovery(title,copy,action){workspace.hidden=true;recovery.hidden=false;recoveryTitle.textContent=title;recoveryCopy.textContent=copy;clearRecoveryActions();if(action==='signin')recoveryActions.append(recoveryLink('Sign in to check in'));if(action==='expired')recoveryActions.append(recoveryLink('Open Profile and sign in again'));if(action==='retry')recoveryActions.append(retryButton())}function showWorkspace(){recovery.hidden=true;workspace.hidden=false}
    function dateLabel(value){if(!value)return'Date TBD';const d=new Date(value+'T12:00:00');return new Intl.DateTimeFormat(undefined,{weekday:'short',month:'short',day:'numeric'}).format(d)}function groupKey(context){return context.seasonId+'|'+context.scheduledOn}function groupContexts(rows){const groups=new Map();for(const context of rows||[]){const key=groupKey(context);if(!groups.has(key))groups.set(key,{key,seasonId:context.seasonId,scheduledOn:context.scheduledOn,contexts:[]});groups.get(key).contexts.push(context)}return Array.from(groups.values()).sort((a,b)=>String(a.scheduledOn).localeCompare(String(b.scheduledOn)))}function contextSummary(group){const labels=[];for(const context of group.contexts){const base='Round '+context.roundNumber;if(context.participationType==='roster'){const team=context.teamName||'Your team';const table=context.tableNumber?' · Table '+context.tableNumber:'';labels.push(base+' · '+team+table)}else labels.push(base+' · Free agent / substitute')}return Array.from(new Set(labels)).join(' • ')}
    function statusLabel(value){return({available:'Available',unsure:'Unsure',unavailable:'Unavailable'})[value]||'Not marked'}function setRowState(card,value){const state=value||'unmarked';card.dataset.state=state;const response=card.querySelector('[data-response]');response.dataset.state=state;response.textContent=statusLabel(value);for(const button of card.querySelectorAll('[data-value]'))button.setAttribute('aria-pressed',String(button.dataset.value===value))}function setRowEnabled(card,enabled){for(const button of card.querySelectorAll('[data-value]'))button.disabled=!enabled}function setRowMessage(card,message,tone){const el=card.querySelector('[data-row-status]');el.textContent=message||'';el.dataset.tone=tone||'muted'}
    function createDateCard(group){const card=document.createElement('article');card.className='date-card';card.setAttribute('role','row');card.dataset.groupKey=group.key;card.dataset.state='unmarked';const copy=document.createElement('div');copy.className='date-copy';copy.setAttribute('role','cell');const title=document.createElement('strong');title.textContent=dateLabel(group.scheduledOn);const detail=document.createElement('span');detail.textContent=contextSummary(group);detail.title=detail.textContent;copy.append(title,detail);const response=document.createElement('span');response.className='response';response.setAttribute('role','cell');response.dataset.response='';response.dataset.state='unmarked';response.textContent='Not marked';const actions=document.createElement('div');actions.className='quick-actions';actions.setAttribute('role','group');actions.setAttribute('aria-label','Availability for '+dateLabel(group.scheduledOn));for(const item of [{value:'available',label:'Yes',ariaLabel:'Available'},{value:'unsure',label:'?',ariaLabel:'Unsure'},{value:'unavailable',label:'No',ariaLabel:'Unavailable'}]){const button=document.createElement('button');button.type='button';button.dataset.value=item.value;button.textContent=item.label;button.setAttribute('aria-label',item.ariaLabel+' for '+dateLabel(group.scheduledOn));button.setAttribute('aria-pressed','false');button.addEventListener('click',()=>run(()=>saveAvailability(group,card,item.value)));actions.append(button)}const rowStatus=document.createElement('div');rowStatus.className='row-status';rowStatus.dataset.rowStatus='';rowStatus.setAttribute('role','status');rowStatus.setAttribute('aria-live','polite');card.append(copy,response,actions,rowStatus);return card}
    function renderContexts(rows){contexts=rows||[];dateList.replaceChildren();const groups=groupContexts(contexts);emptyEl.hidden=groups.length>0;if(!groups.length){setStatus('No upcoming league night available yet.');return[]}for(const group of groups)dateList.append(createDateCard(group));return groups}
    async function loadSavedAvailability(group,card){setRowEnabled(card,false);setRowMessage(card,'Loading saved response…');const body=await signedApi('/api/seasons/'+encodeURIComponent(group.seasonId)+'/availability/me?date='+encodeURIComponent(group.scheduledOn),{method:'GET'});const availability=body.availability||{};setRowState(card,availability.availability_status||null);if(availability.registered===false){setRowEnabled(card,false);setRowMessage(card,'Action needed: join this season before checking in.','error');return}setRowEnabled(card,true);setRowMessage(card,availability.availability_status?'':'Not answered yet')}
    async function saveAvailability(group,card,value){setRowEnabled(card,false);setRowMessage(card,'Saving…');const body=await signedApi('/api/seasons/'+encodeURIComponent(group.seasonId)+'/availability/me',{method:'PUT',body:JSON.stringify({date:group.scheduledOn,status:value})});setRowState(card,body.availability?.availability_status||value);setRowEnabled(card,true);setRowMessage(card,'','ok');setStatus('Saved. Your captain can see your answers.','ok')}
    function choiceTeamName(row,teamId){return row.team_a_id===teamId?row.team_a_name:row.team_b_name}function renderTeamChoices(rows){teamChoices=rows||[];teamChoiceList.replaceChildren();teamChoicePanel.hidden=teamChoices.length===0;for(const row of teamChoices){const card=document.createElement('article');card.className='choice-card';const title=document.createElement('strong');title.textContent='Round '+row.round_number+' · '+row.team_a_name+' vs '+row.team_b_name;const copy=document.createElement('div');copy.className='choice-copy';const date=document.createElement('span');date.textContent=dateLabel(row.scheduled_on)+(row.stage&&row.stage!=='regular'?' · '+row.stage:'');const explanation=document.createElement('span');explanation.textContent=row.selected_team_id?('You chose '+choiceTeamName(row,row.selected_team_id)+'.'):'You belong to both teams. Choose which team you will represent before captains build lineups.';copy.append(date,explanation);const actions=document.createElement('div');actions.className='choice-actions';for(const team of [{id:row.team_a_id,name:row.team_a_name},{id:row.team_b_id,name:row.team_b_name}]){const button=document.createElement('button');button.type='button';button.className='choice-team'+(row.selected_team_id===team.id?' selected':'');button.textContent=(row.selected_team_id===team.id?'Chosen: ':'Play for ')+team.name;button.disabled=Boolean(row.choice_locked)||row.selected_team_id===team.id;button.dataset.chooseTeam=team.id;button.dataset.teamMatch=row.team_match_id;actions.append(button)}card.append(title,copy,actions);if(row.choice_locked){const note=document.createElement('div');note.className='choice-note';note.textContent='Choice locked because a lineup already includes you.';card.append(note)}teamChoiceList.append(card)}}
    async function loadTeamChoices(){const body=await signedApi('/api/me/team-match-choices',{method:'GET'});renderTeamChoices(body.choices||[])}async function saveTeamChoice(teamMatchId,teamId){setStatus('Saving your team choice…');await signedApi('/api/team-matches/'+encodeURIComponent(teamMatchId)+'/team-choice/me',{method:'PUT',body:JSON.stringify({teamId})});await loadTeamChoices();setStatus('Team choice saved.','ok')}
    async function loadContexts(){setStatus('Loading your league nights…');const body=await signedApi('/api/me/teams',{method:'GET'});return renderContexts(body.teamManagement?.availability_contexts||[])}async function loadPage(){if(!accessToken()){setStatus('Sign in to check in.');showRecovery('Check in','Sign in to tell your captain whether you can play or to volunteer as a substitute.','signin');return}showRecovery('Loading your league nights…','Checking your upcoming rounds and team choices.');const groups=await loadContexts();await loadTeamChoices();showWorkspace();if(!groups.length)return;setStatus('Loading your saved responses…');await Promise.all(groups.map((group)=>{const card=dateList.querySelector('[data-group-key="'+CSS.escape(group.key)+'"]');return loadSavedAvailability(group,card)}));setStatus('Tap any date to update your response.','ok')}
    async function run(action){try{await action()}catch(error){const message=error?.message||'Check-in could not be loaded.';setStatus(message,'error');if(message.startsWith('Your sign-in expired'))showRecovery('Your sign-in expired','Sign in again to keep your answer tied to the correct player.','expired');else if(recovery.hidden===false)showRecovery('Check-in needs attention',message,'retry')}}
    teamChoiceList.addEventListener('click',(event)=>{const button=event.target.closest('[data-choose-team]');if(!button)return;run(()=>saveTeamChoice(button.dataset.teamMatch,button.dataset.chooseTeam))});run(loadPage);
  </script>
</body>
</html>`;
}
