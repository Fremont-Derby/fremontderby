export function renderStandingsPage() {
  return `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Fremont Derby Standings</title>
  <style>
    :root{color-scheme:dark;font-family:Inter,ui-sans-serif,system-ui,sans-serif;background:#101214;color:#f5f0e8;--panel:#191c1f;--panel2:#111517;--line:#343b42;--muted:#aab2b8;--green:#2ea86f;--gold:#d9ad38;--focus:#8ee9b8}
    *{box-sizing:border-box}body{margin:0;min-height:100vh;background:radial-gradient(circle at 50% 0,#18221d,#101214 34rem);color:#f5f0e8}button,select{font:inherit}button{min-height:44px;border-radius:9px;border:1px solid var(--line);cursor:pointer;font-weight:850}button:disabled{cursor:not-allowed;opacity:.55}.app{width:min(1120px,100%);margin:0 auto;padding:16px}.topbar{display:flex;justify-content:space-between;gap:12px;align-items:center;padding-bottom:12px}.brand{display:flex;align-items:center;gap:10px;font-weight:950;font-size:1.05rem}.mark{width:34px;height:34px;border-radius:50%;display:grid;place-items:center;background:linear-gradient(90deg,#f5f0e8 0 35%,var(--gold) 35% 65%,#f5f0e8 65%);color:#101214;font-weight:950;box-shadow:0 2px 0 #725b1f}.status{min-height:24px;color:var(--muted);text-align:right;font-size:.86rem}.status[data-tone="error"]{color:#ffb1aa}.status[data-tone="ok"]{color:#9ee5bd}.tabs-wrap{position:sticky;top:0;z-index:5;padding:8px 0 10px;background:#101214;border-bottom:1px solid var(--line)}.tabs{display:grid;grid-template-columns:1fr 1fr;gap:6px;padding:4px;border:1px solid var(--line);border-radius:12px;background:#0d0f11}.tab{min-height:48px;background:transparent;color:#f5f0e8;border-color:transparent;padding:0 14px}.tab[aria-selected="true"]{background:var(--green);color:#06120d;border-color:var(--green);box-shadow:0 2px 0 #17653f}.tab:focus-visible,.load:focus-visible,.register-link:focus-visible,.state-action:focus-visible,select:focus-visible{outline:3px solid var(--focus);outline-offset:3px}.controls{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;padding:14px 0}label{display:grid;gap:6px;color:var(--muted);font-size:.78rem;font-weight:850}select{width:100%;min-height:46px;border:1px solid var(--line);border-radius:9px;background:#0d0f11;color:#f5f0e8;padding:0 12px}.load{align-self:end;background:var(--gold);color:#101214;border-color:transparent;padding:0 18px;box-shadow:0 2px 0 #725b1f}.state-card{display:grid;gap:10px;margin:0 0 14px;padding:18px;border:1px solid var(--line);border-top:4px solid var(--gold);border-radius:12px;background:linear-gradient(145deg,#1c2220,#15191a)}.state-card[hidden]{display:none}.state-card strong{font-size:1.05rem}.state-card p{margin:0;color:var(--muted);line-height:1.5}.state-action{width:max-content;min-height:44px;display:inline-flex;align-items:center;justify-content:center;padding:0 14px;border:1px solid var(--green);border-radius:9px;background:var(--green);color:#06120d;text-decoration:none;font-weight:900}.summary{display:grid;grid-template-columns:repeat(3,minmax(0,1fr)) auto;gap:12px;align-items:center;margin:0 0 14px;padding:14px;border:1px solid var(--line);border-radius:12px;background:linear-gradient(145deg,#1c2220,#15191a);box-shadow:inset 0 1px rgba(255,255,255,.04)}.summary[hidden],.panel[hidden]{display:none}.metric{display:grid;gap:3px}.metric strong{color:var(--gold);font-size:1.2rem}.metric span{color:var(--muted);font-size:.78rem;font-weight:800}.register-link{min-height:44px;display:inline-flex;align-items:center;justify-content:center;padding:0 14px;border-radius:9px;background:var(--green);color:#06120d;text-decoration:none;font-weight:900;white-space:nowrap;box-shadow:0 2px 0 #17653f}.panel{border:1px solid var(--line);border-radius:12px;overflow:hidden;background:var(--panel)}table{width:100%;border-collapse:collapse;table-layout:fixed}th,td{padding:12px;border-bottom:1px solid var(--line);text-align:left;vertical-align:middle}th{color:var(--muted);font-size:.75rem;text-transform:uppercase}td{overflow-wrap:anywhere}tr:last-child td{border-bottom:0}.rank{width:72px;color:var(--gold);font-weight:950}.numeric{text-align:right;font-variant-numeric:tabular-nums}.badge{display:inline-flex;align-items:center;min-height:28px;border-radius:999px;padding:0 10px;font-size:.78rem;font-weight:900;background:#263039;color:#d8e4ea}.badge.ok{background:rgba(46,168,111,.18);color:#9ee5bd}.badge.warn{background:rgba(217,173,56,.18);color:#f0d48b}.empty{padding:20px;color:var(--muted);line-height:1.45}.mobile-list{display:none}.standing-card{padding:14px;border-bottom:1px solid var(--line);background:linear-gradient(145deg,var(--panel),var(--panel2))}.standing-card:last-child{border-bottom:0}.card-head{display:grid;grid-template-columns:auto 1fr;gap:10px;align-items:center;margin-bottom:12px}.card-rank{min-width:42px;height:42px;display:grid;place-items:center;border:1px solid #6c5721;border-radius:50%;background:#2b2516;color:#f3d87e;font-weight:950;font-variant-numeric:tabular-nums}.card-name{font-size:1.05rem;font-weight:950;line-height:1.2}.card-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.card-stat{min-width:0;padding:9px 10px;border:1px solid #2f3738;border-radius:9px;background:#111516}.card-stat span{display:block;margin-bottom:3px;color:var(--muted);font-size:.7rem;font-weight:850;text-transform:uppercase;letter-spacing:.02em}.card-stat strong{display:block;font-size:.95rem;overflow-wrap:anywhere}.card-stat.prize{grid-column:1/-1}.mobile-empty{display:none}
    @media(max-width:760px){.app{padding:12px}.topbar{align-items:flex-start;flex-direction:column}.status{text-align:left}.controls,.summary{grid-template-columns:1fr}.tab{padding:0 8px}.panel table{display:none}.mobile-list{display:block}.panel{overflow:hidden}.mobile-empty{display:block}.panel>.empty{display:none}.summary{gap:10px}.metric{padding:8px 0;border-bottom:1px solid #30383a}.metric:nth-child(3){border-bottom:0}.register-link,.state-action{width:100%;margin-top:2px}.tabs-wrap{top:58px}}
    @media(prefers-reduced-motion:reduce){*,*::before,*::after{scroll-behavior:auto!important;transition:none!important;animation:none!important}}
  </style>
</head>
<body>
  <main class="app">
    <header class="topbar"><div class="brand"><span class="mark">9</span><span>Fremont Derby Standings</span></div><div class="status" data-status aria-live="polite">Loading seasons…</div></header>
    <div class="tabs-wrap"><nav class="tabs" role="tablist" aria-label="Standings views"><button class="tab" data-tab="teams" role="tab" type="button" aria-selected="true" aria-controls="team-standings">Team standings</button><button class="tab" data-tab="individuals" role="tab" type="button" aria-selected="false" aria-controls="individual-standings">Individual standings</button></nav></div>
    <form class="controls" data-form><label>Season<select name="seasonId" data-season-id><option value="">Loading seasons…</option></select></label><button class="load" data-load type="submit">Load standings</button></form>
    <section class="state-card" data-page-state hidden aria-live="polite"><strong data-state-title></strong><p data-state-detail></p><a class="state-action" data-state-action href="/standings"></a></section>
    <section class="summary" data-registration-summary hidden><div class="metric"><strong data-team-count>0 / 8</strong><span>Teams registered</span></div><div class="metric"><strong data-player-count>0</strong><span>Rostered players</span></div><div class="metric"><strong data-open-slots>8</strong><span>Open team slots</span></div><a class="register-link" data-register-link href="/teams">Register or join a team</a></section>
    <section class="panel" id="team-standings" role="tabpanel" data-panel="teams" aria-labelledby="team-tab"><table><thead><tr><th class="rank">Rank</th><th>Team</th><th class="numeric">GP</th><th class="numeric">Max</th><th class="numeric">W-L</th><th class="numeric">Pts</th><th class="numeric">Match</th><th class="numeric">Diff</th><th class="numeric">Forfeit</th></tr></thead><tbody data-team-body></tbody></table><div class="mobile-list" data-team-cards aria-label="Team standings"></div><div class="empty" data-team-empty>Standings are loading.</div><div class="empty mobile-empty" data-team-mobile-empty>Standings are loading.</div></section>
    <section class="panel" id="individual-standings" role="tabpanel" data-panel="individuals" hidden><table><thead><tr><th class="rank">Rank</th><th>Player</th><th class="numeric">Record</th><th class="numeric">Win %</th><th class="numeric">Games</th><th class="numeric">Diff</th><th>Prize</th></tr></thead><tbody data-player-body></tbody></table><div class="mobile-list" data-player-cards aria-label="Individual standings"></div><div class="empty" data-player-empty>Standings are loading.</div><div class="empty mobile-empty" data-player-mobile-empty>Standings are loading.</div></section>
  </main>
  <script>
    const form=document.querySelector('[data-form]');const seasonInput=document.querySelector('[data-season-id]');const loadButton=document.querySelector('[data-load]');const statusEl=document.querySelector('[data-status]');const teamBody=document.querySelector('[data-team-body]');const playerBody=document.querySelector('[data-player-body]');const teamCards=document.querySelector('[data-team-cards]');const playerCards=document.querySelector('[data-player-cards]');const teamEmpty=document.querySelector('[data-team-empty]');const playerEmpty=document.querySelector('[data-player-empty]');const teamMobileEmpty=document.querySelector('[data-team-mobile-empty]');const playerMobileEmpty=document.querySelector('[data-player-mobile-empty]');const tabs=Array.from(document.querySelectorAll('[data-tab]'));const panels=Array.from(document.querySelectorAll('[data-panel]'));const pageState=document.querySelector('[data-page-state]');const stateTitle=document.querySelector('[data-state-title]');const stateDetail=document.querySelector('[data-state-detail]');const stateAction=document.querySelector('[data-state-action]');const registrationSummary=document.querySelector('[data-registration-summary]');const teamCountEl=document.querySelector('[data-team-count]');const playerCountEl=document.querySelector('[data-player-count]');const openSlotsEl=document.querySelector('[data-open-slots]');const registerLink=document.querySelector('[data-register-link]');const query=new URLSearchParams(location.search);const requestedSeasonId=query.get('season')||'';const rememberedSeasonId=localStorage.getItem('fd.standingsSeasonId')||'';const requestedView=query.get('view');const rememberedView=localStorage.getItem('fd.standingsView');let seasons=[];
    function setStatus(message,tone){statusEl.textContent=message;statusEl.dataset.tone=tone||'muted'}function cell(textValue,className){const td=document.createElement('td');if(className)td.className=className;td.textContent=textValue==null||textValue===''?'-':String(textValue);return td}function percent(value){const number=Number(value||0);return Math.round(number*1000)/10+'%'}function badge(label,className){const span=document.createElement('span');span.className='badge '+className;span.textContent=label;return span}function showState(title,detail,href,label){stateTitle.textContent=title;stateDetail.textContent=detail;stateAction.href=href;stateAction.textContent=label;pageState.hidden=false}function hideState(){pageState.hidden=true}
    function stat(label,value,className=''){const item=document.createElement('div');item.className='card-stat'+(className?' '+className:'');const name=document.createElement('span');name.textContent=label;const strong=document.createElement('strong');if(value instanceof Node)strong.append(value);else strong.textContent=value==null||value===''?'-':String(value);item.append(name,strong);return item}
    function card(rank,name,stats){const article=document.createElement('article');article.className='standing-card';const head=document.createElement('div');head.className='card-head';const rankEl=document.createElement('div');rankEl.className='card-rank';rankEl.setAttribute('aria-label','Rank '+String(rank));rankEl.textContent=String(rank);const title=document.createElement('div');title.className='card-name';title.textContent=name||'Unnamed';head.append(rankEl,title);const grid=document.createElement('div');grid.className='card-grid';grid.append(...stats);article.append(head,grid);return article}
    function selectTab(name,persist=true){const selected=name==='individuals'?'individuals':'teams';for(const tab of tabs){const active=tab.dataset.tab===selected;tab.setAttribute('aria-selected',String(active));tab.tabIndex=active?0:-1}for(const panel of panels)panel.hidden=panel.dataset.panel!==selected;if(persist){localStorage.setItem('fd.standingsView',selected);const next=new URL(location.href);next.searchParams.set('view',selected);history.replaceState(null,'',next.pathname+next.search+next.hash)}}
    function renderSeasonOptions(){seasonInput.replaceChildren();for(const season of seasons){const option=document.createElement('option');option.value=season.id;option.textContent=season.name+' — '+season.status;seasonInput.append(option)}const explicit=seasons.find((season)=>season.id===requestedSeasonId);const registration=seasons.find((season)=>season.status==='registration');const remembered=seasons.find((season)=>season.id===rememberedSeasonId);const selected=explicit||remembered||registration||seasons[0];seasonInput.value=selected?.id||'';seasonInput.disabled=seasons.length===0;loadButton.disabled=seasons.length===0}
    function renderRegistrationSummary(season){const isRegistration=season?.status==='registration';registrationSummary.hidden=!isRegistration;if(!isRegistration)return;const openSlots=Number(season.openTeamSlots||0);teamCountEl.textContent=season.teamCount+' / '+season.teamCapacity;playerCountEl.textContent=String(season.rosteredPlayerCount);openSlotsEl.textContent=String(season.openTeamSlots);registerLink.href='/teams?season='+encodeURIComponent(season.id);registerLink.textContent=openSlots<=0?(season.applicationsWaiting?'View waitlist / teams':'Teams full — view teams'):'Register or join a team'}
    async function loadSeasons(){setStatus('Loading seasons...');hideState();const response=await fetch('/api/seasons');const body=await response.json();if(!response.ok)throw new Error((body&&body.error)||('Standings could not be loaded (HTTP '+response.status+').'));seasons=body.seasons||[];renderSeasonOptions();if(!seasons.length){renderRegistrationSummary(null);renderTeams([]);renderPlayers([]);const message='No season is published yet. Standings will appear here once league setup is ready.';teamEmpty.textContent=message;teamMobileEmpty.textContent=message;playerEmpty.textContent=message;playerMobileEmpty.textContent=message;showState('No season yet','There is nothing to rank yet. You can still review the league format while registration and scheduling are prepared.','/rules','View league rules');setStatus('No season published yet');return false}return true}
    function rankLabel(rows,index){const rank=rows[index]?.standings_rank;if(rank==null||rank==='')return'-';const tied=rows.filter((row)=>row.standings_rank===rank).length>1;return tied?'T-'+rank:String(rank)}function renderTeams(rows){teamBody.replaceChildren();teamCards.replaceChildren();teamEmpty.hidden=rows.length>0;teamMobileEmpty.hidden=rows.length>0;rows.forEach((row,index)=>{const tr=document.createElement('tr');tr.append(cell(rankLabel(rows,index),'rank'),cell(row.team_name),cell(row.games_played,'numeric'),cell(row.maximum_matches,'numeric'),cell([row.team_wins,row.team_losses].join('-'),'numeric'),cell(row.standing_points,'numeric'),cell(row.match_points+'-'+row.match_points_against,'numeric'),cell(row.point_differential,'numeric'),cell(row.forfeits_won+'-'+row.forfeits_lost,'numeric'));teamBody.append(tr);teamCards.append(card(rankLabel(rows,index),row.team_name,[stat('Record',row.team_wins+'-'+row.team_losses),stat('Points',row.standing_points),stat('Played',row.games_played+' of '+row.maximum_matches),stat('Match points',row.match_points+'-'+row.match_points_against),stat('Differential',row.point_differential),stat('Forfeits',row.forfeits_won+'-'+row.forfeits_lost)]))})
    function renderPlayers(rows,season){const isRegistration=season&&season.status==='registration';const isComplete=season&&season.status==='complete';let list=rows.slice();if(isRegistration||isComplete){list=list.filter((row)=>(Number(row.matches_played)||(Number(row.wins)+Number(row.losses)))>0)}playerBody.replaceChildren();playerCards.replaceChildren();playerEmpty.hidden=list.length>0;playerMobileEmpty.hidden=list.length>0;list.forEach((row,index)=>{const tr=document.createElement('tr');const prize=document.createElement('td');const played=(Number(row.matches_played)||(Number(row.wins)+Number(row.losses)))>0;const showPrize=played&&row.is_prize_eligible;prize.append(showPrize?badge('Eligible #'+row.prize_rank,'ok'):(played?badge('Needs '+row.minimum_matches,'warn'):badge('—','muted')));tr.append(cell(rankLabel(list,index),'rank'),cell(row.display_name),cell(row.wins+'-'+row.losses,'numeric'),cell(percent(row.win_percentage),'numeric'),cell(row.games_won+'-'+row.games_lost,'numeric'),cell(row.game_differential,'numeric'),prize);playerBody.append(tr);const prizeBadge=showPrize?badge('Eligible #'+row.prize_rank,'ok'):(played?badge('Needs '+row.minimum_matches,'warn'):badge('—','muted'));playerCards.append(card(rankLabel(list,index),row.display_name,[stat('Record',row.wins+'-'+row.losses),stat('Win rate',percent(row.win_percentage)),stat('Games',row.games_won+'-'+row.games_lost),stat('Differential',row.game_differential),stat('Prize status',prizeBadge,'prize')]))})
    async function loadStandings(){const seasonId=seasonInput.value.trim();if(!seasonId)throw new Error('Choose a season first.');localStorage.setItem('fd.standingsSeasonId',seasonId);hideState();setStatus('Loading standings...');const encoded=encodeURIComponent(seasonId);const[teamResponse,playerResponse]=await Promise.all([fetch('/api/seasons/'+encoded+'/team-standings'),fetch('/api/seasons/'+encoded+'/individual-standings')]);const teamBodyJson=await teamResponse.json();const playerBodyJson=await playerResponse.json();if(!teamResponse.ok||!playerResponse.ok)throw new Error((teamBodyJson&&teamBodyJson.error)||(playerBodyJson&&playerBodyJson.error)||('Standings could not be loaded (HTTP '+teamResponse.status+'/'+playerResponse.status+').'));const teamRows=teamBodyJson.standings||[];const playerRows=playerBodyJson.standings||[];const season=seasons.find((candidate)=>candidate.id===seasonId);renderTeams(teamRows);renderPlayers(playerRows,season);renderRegistrationSummary(season);const teamMessage=!teamRows.length&&season?.status==='registration'?season.teamCount+' of '+season.teamCapacity+' teams are registered. Standings begin after league play starts.':'No team standings are available for this season.';const playerMessage=!playerRows.length&&season?.status==='registration'?'Player standings begin after scored matches.':'No individual standings are available for this season.';teamEmpty.textContent=teamMessage;teamMobileEmpty.textContent=teamMessage;playerEmpty.textContent=playerMessage;playerMobileEmpty.textContent=playerMessage;setStatus(season?.status==='registration'?'Registration progress loaded':'Standings loaded','ok')}
    async function run(action){try{await action()}catch(error){setStatus('Could not load standings','error');showState('Standings unavailable','We could not load this public standings view. Nothing needs to be re-entered.','/standings','Try again')}}seasonInput.addEventListener('change',()=>{if(seasonInput.value)run(loadStandings)});form.addEventListener('submit',(event)=>{event.preventDefault();run(loadStandings)});for(const tab of tabs){tab.addEventListener('click',()=>selectTab(tab.dataset.tab));tab.addEventListener('keydown',(event)=>{if(event.key!=='ArrowLeft'&&event.key!=='ArrowRight')return;event.preventDefault();const next=tab.dataset.tab==='teams'?'individuals':'teams';selectTab(next);tabs.find((item)=>item.dataset.tab===next)?.focus()})}selectTab(requestedView||rememberedView||'teams',false);run(async()=>{const hasSeasons=await loadSeasons();if(hasSeasons)await loadStandings()});
  </script>
</body>
</html>`;
}
