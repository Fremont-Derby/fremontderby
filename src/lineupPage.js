export function renderLineupPage() {
  return `<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Fremont Derby Lineup</title>
  <style>
    :root{color-scheme:dark;font-family:Inter,ui-sans-serif,system-ui,sans-serif;background:#111316;color:#f5f1e9;--panel:#191d22;--line:#343c45;--muted:#aab3bb;--green:#2fa972;--gold:#d8ad3f;--red:#d45b50}
    *{box-sizing:border-box}body{margin:0;min-height:100vh;background:#111316}button,input,select{font:inherit}button{min-height:44px;border:1px solid transparent;border-radius:9px;font-weight:850;cursor:pointer}button:disabled,select:disabled{cursor:not-allowed;opacity:.5}input,select{width:100%;min-height:44px;border:1px solid var(--line);border-radius:9px;background:#0d1013;color:#f5f1e9;padding:0 12px}.app{width:min(1100px,100%);margin:auto;padding:14px}.topbar{display:flex;justify-content:space-between;gap:12px;align-items:center;padding-bottom:14px;border-bottom:1px solid var(--line)}.brand{display:flex;align-items:center;gap:10px;font-weight:950}.mark{width:34px;height:34px;border-radius:9px;display:grid;place-items:center;background:var(--green);color:#06120d;font-weight:950}.status{color:var(--muted);text-align:right;min-height:30px}.status-close{display:inline-grid;place-items:center;min-width:44px;min-height:44px;margin-left:10px;border-color:#8b4540;background:#522522;color:#fff}.status[data-tone="error"]{position:fixed;z-index:50;top:12px;left:50%;transform:translateX(-50%);display:flex;align-items:center;justify-content:space-between;width:min(520px,calc(100% - 24px));padding:10px 10px 10px 14px;border:1px solid #8b4540;border-radius:12px;background:#351a1a;box-shadow:0 12px 34px rgba(0,0,0,.55);color:#ffcbc6;text-align:left}.status[data-tone="ok"]{color:#9ee5bd}.setup{display:grid;grid-template-columns:1fr 1fr auto;gap:10px;align-items:end;padding:14px 0}.setup label,.search-label{display:grid;gap:6px;color:var(--muted);font-size:.78rem;font-weight:900}.load{background:var(--gold);color:#12100a}.layout{display:grid;grid-template-columns:minmax(300px,.9fr) minmax(0,1.1fr);gap:14px}.panel{border:1px solid var(--line);border-radius:12px;background:var(--panel);overflow:hidden}.panel-head{min-height:52px;display:flex;justify-content:space-between;align-items:center;gap:10px;padding:0 12px;border-bottom:1px solid var(--line);font-weight:950}.badge{display:inline-flex;align-items:center;min-height:28px;padding:0 9px;border-radius:999px;background:#26303a;color:#d8e4ea;font-size:.76rem;font-weight:900}.badge.available,.badge.paid{background:rgba(47,169,114,.18);color:#9ee5bd}.badge.unavailable,.badge.blocked{background:rgba(212,91,80,.18);color:#ffb1aa}.badge.unsure{background:rgba(216,173,63,.18);color:#f0d48b}.picker{display:grid;gap:10px;padding:12px}.mobile-lineup-summary{display:none}.section-title{display:flex;justify-content:space-between;align-items:center;color:#dbe4df;font-size:.82rem;font-weight:950;text-transform:uppercase;letter-spacing:.04em}.player-list{display:grid;gap:8px}.player{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:10px;align-items:center;padding:11px;border:1px solid var(--line);border-radius:10px;background:#12171b}.player[data-blocked="true"]{opacity:.62}.player-name{font-weight:950}.meta{display:flex;flex-wrap:wrap;gap:6px;margin-top:5px;color:var(--muted);font-size:.78rem}.add{background:var(--green);color:#06120d;padding:0 13px}.empty{padding:14px;color:var(--muted)}.slots{display:grid;gap:10px;padding:12px}.slot{display:grid;grid-template-columns:44px minmax(0,1fr) auto;gap:9px;align-items:center;min-height:72px;padding:10px;border:1px solid var(--line);border-radius:10px;background:#12171b}.slot-number{width:38px;height:38px;border-radius:9px;display:grid;place-items:center;background:#222932;color:var(--gold);font-weight:950}.slot-main strong{display:block}.slot-main span{display:block;margin-top:3px;color:var(--muted);font-size:.8rem}.slot-actions{display:flex;gap:6px}.slot-actions button{min-height:36px;padding:0 9px;background:transparent;border-color:var(--line);color:#f5f1e9}.slot-actions .remove{color:#ffb1aa}.forfeit{width:100%;background:transparent;border-color:var(--line);color:#f0d48b}.lock-note{padding:0 12px 12px;color:#9ee5bd;font-size:.88rem}.actions{display:grid;grid-template-columns:1fr 1fr;gap:10px;padding:0 12px 12px}.submit,.mobile-lineup-lock{background:var(--green);color:#06120d}.refresh{background:transparent;border-color:var(--line);color:#f5f1e9}.reveal{margin-top:14px}.committed{display:grid;gap:8px;padding:12px}.committed-row{display:grid;grid-template-columns:74px 40px minmax(0,1fr);gap:8px;padding:9px;border-bottom:1px solid var(--line)}.committed-row:last-child{border-bottom:0}.score-link{display:inline-flex;margin:0 12px 12px;min-height:42px;align-items:center;padding:0 12px;border-radius:9px;background:var(--gold);color:#17120a;text-decoration:none;font-weight:950}.hint{padding:0 12px 12px;color:var(--muted);font-size:.82rem}.signin{display:inline-flex;align-items:center;min-height:44px;padding:0 12px;border:1px solid var(--green);border-radius:9px;color:#9ee5bd;text-decoration:none;font-weight:900}
    @media(max-width:800px){.topbar{align-items:flex-start}.status{text-align:left}.setup,.layout,.actions{grid-template-columns:1fr}.picker-panel{overflow:visible}.mobile-lineup-summary{position:sticky;top:70px;z-index:20;display:grid;gap:8px;padding:10px;border:1px solid var(--line);border-radius:11px;background:rgba(18,23,27,.97);box-shadow:0 10px 26px rgba(0,0,0,.35);backdrop-filter:blur(10px)}.mobile-lineup-summary-head{display:flex;align-items:center;justify-content:space-between;gap:10px}.mobile-lineup-summary-head strong{font-size:.86rem}.mobile-lineup-summary-slots{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:6px}.mobile-slot{min-width:0;padding:8px;border:1px solid var(--line);border-radius:8px;background:#0d1013}.mobile-slot strong{display:block;color:var(--gold);font-size:.7rem;text-transform:uppercase}.mobile-slot span{display:block;margin-top:3px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;font-size:.78rem;font-weight:850}.mobile-lineup-lock{width:100%}.player{grid-template-columns:1fr auto}.slot{grid-template-columns:42px minmax(0,1fr)}.slot-actions{grid-column:2;justify-content:flex-start}.setup{padding-top:10px}}
  </style>
</head>
<body>
  <main class="app">
    <header class="topbar"><div class="brand"><span class="mark">L</span><span>Fremont Derby Lineup</span></div><div class="status" data-status role="status" aria-live="polite"><span data-status-copy>Loading…</span><button class="status-close" data-status-close type="button" aria-label="Dismiss message" hidden>Close</button></div></header>
    <form class="setup" data-form>
      <label>Team<select data-team-select><option value="">Loading your teams…</option></select></label>
      <label>Matchup<select data-round-select disabled><option value="">Choose a team first</option></select></label>
      <button class="load" data-load type="submit">Open lineup</button>
    </form>

    <section class="layout">
      <article class="panel picker-panel">
        <div class="panel-head"><span>Pick tonight's players</span><span class="badge" data-availability-count>0</span></div>
        <div class="picker">
          <section class="mobile-lineup-summary" data-mobile-lineup-summary aria-label="Current lineup">
            <div class="mobile-lineup-summary-head"><strong>Your three slots</strong><span class="badge" data-mobile-slot-count>0 / 3</span></div>
            <div class="mobile-lineup-summary-slots" data-mobile-lineup-slots aria-live="polite"></div>
            <button class="mobile-lineup-lock" data-mobile-submit type="button">Lock lineup</button>
          </section>
          <label class="search-label">Search players<input data-player-search type="search" placeholder="Name" autocomplete="off" /></label>
          <div class="section-title"><span>Roster</span><span data-roster-count>0</span></div>
          <div class="player-list" data-roster-list></div>
          <div class="section-title"><span>Available substitutes</span><span data-sub-count>0</span></div>
          <div class="player-list" data-sub-list></div>
          <div data-availability-body hidden></div>
        </div>
      </article>

      <article class="panel">
        <div class="panel-head"><span>Blind ordered lineup</span><span class="badge" data-slot-count>0 / 3</span></div>
        <div class="slots" data-slots></div>
        <div class="hint">Choose exactly three slots. A blank slot must be explicitly marked Forfeit. Opponent order stays hidden until both teams submit.</div>
        <div class="lock-note" data-lock-note hidden>Your lineup is locked. Opponent order stays hidden until both teams submit.</div>
        <div class="actions"><button class="submit" data-submit type="button">Lock lineup</button><button class="refresh" data-refresh type="button">Refresh</button></div>
      </article>
    </section>

    <section class="panel reveal">
      <div class="panel-head"><span>Committed lineups</span><span class="badge" data-lineup-count>0</span></div>
      <div class="committed" data-lineup-body></div>
      <div class="empty" data-lineup-empty>No lineup committed yet.</div>
      <a class="score-link" data-score-link href="/scorecard" hidden>Score the three matches</a>
    </section>
  </main>
  <script>
    const form=document.querySelector('[data-form]');const teamSelect=document.querySelector('[data-team-select]');const roundSelect=document.querySelector('[data-round-select]');const loadButton=document.querySelector('[data-load]');const statusEl=document.querySelector('[data-status]');const statusCopy=document.querySelector('[data-status-copy]');const statusClose=document.querySelector('[data-status-close]');const rosterList=document.querySelector('[data-roster-list]');const subList=document.querySelector('[data-sub-list]');const rosterCount=document.querySelector('[data-roster-count]');const subCount=document.querySelector('[data-sub-count]');const availabilityCount=document.querySelector('[data-availability-count]');const searchInput=document.querySelector('[data-player-search]');const lineupBody=document.querySelector('[data-lineup-body]');const lineupEmpty=document.querySelector('[data-lineup-empty]');const lineupCount=document.querySelector('[data-lineup-count]');const slotsEl=document.querySelector('[data-slots]');const slotCount=document.querySelector('[data-slot-count]');const submitButton=document.querySelector('[data-submit]');const mobileSlotsEl=document.querySelector('[data-mobile-lineup-slots]');const mobileSlotCount=document.querySelector('[data-mobile-slot-count]');const mobileSubmitButton=document.querySelector('[data-mobile-submit]');const refreshButton=document.querySelector('[data-refresh]');const lockNote=document.querySelector('[data-lock-note]');const scoreLink=document.querySelector('[data-score-link]');let captainTeams=[];let availabilityRows=[];let lineupLocked=false;let selectedSlots=[null,null,null];
    const params=new URLSearchParams(location.search);const requestedTeam=params.get('team')||localStorage.getItem('fd.lineupTeamId')||'';const requestedRound=params.get('round')||localStorage.getItem('fd.lineupRoundId')||'';
    function setStatus(message,tone){statusCopy.textContent=message;statusEl.dataset.tone=tone||'muted';const isError=tone==='error';statusClose.hidden=!isError;statusEl.setAttribute('role',isError?'alert':'status')}function accessToken(){return sessionStorage.getItem('fd.accessToken')||''}function currentContext(){const teamId=teamSelect.value;const roundId=roundSelect.value;const token=accessToken();if(!token)throw new Error('Sign in with Google to manage a lineup.');if(!teamId)throw new Error('Choose a team.');if(!roundId)throw new Error('Choose a scheduled round.');localStorage.setItem('fd.lineupTeamId',teamId);localStorage.setItem('fd.lineupRoundId',roundId);return{teamId,roundId,token}}async function parseJson(response){const text=await response.text();if(!text)return{};try{return JSON.parse(text)}catch{return{error:text}}}async function api(path,options={}){const context=currentContext();const url=path.replace(':teamId',encodeURIComponent(context.teamId)).replace(':roundId',encodeURIComponent(context.roundId));const response=await fetch(url,{...options,headers:{authorization:'Bearer '+context.token,'content-type':'application/json',...(options.headers||{})}});const body=await parseJson(response);if(response.status===401){sessionStorage.removeItem('fd.accessToken');throw new Error('Your sign-in expired. Open Profile and sign in again.')}if(!response.ok)throw new Error(body.error||'Request failed');return body}async function meApi(path){const token=accessToken();if(!token)throw new Error('Sign in with Google to manage a lineup.');const response=await fetch(path,{headers:{authorization:'Bearer '+token}});const body=await parseJson(response);if(response.status===401){sessionStorage.removeItem('fd.accessToken');throw new Error('Your sign-in expired. Open Profile and sign in again.')}if(!response.ok)throw new Error(body.error||'Request failed');return body}
    function dateLabel(value){if(!value)return'Date TBD';const d=new Date(value+'T12:00:00');return new Intl.DateTimeFormat(undefined,{weekday:'short',month:'short',day:'numeric'}).format(d)}function activeTeam(){return captainTeams.find((team)=>team.teamId===teamSelect.value)||null}function preferredRound(rounds){if(requestedRound&&rounds.some((round)=>round.roundId===requestedRound))return requestedRound;const today=new Date().toISOString().slice(0,10);const upcoming=rounds.find((round)=>!['finalized','corrected'].includes(round.teamMatchStatus)&&(!round.scheduledOn||round.scheduledOn>=today));return(upcoming||rounds.find((round)=>!['finalized','corrected'].includes(round.teamMatchStatus))||rounds[0]||{}).roundId||''}
    function renderTeamOptions(){teamSelect.replaceChildren();if(!captainTeams.length){const option=document.createElement('option');option.value='';option.textContent='No captained teams';teamSelect.append(option);teamSelect.disabled=true;roundSelect.disabled=true;loadButton.disabled=true;submitButton.disabled=true;mobileSubmitButton.disabled=true;return}for(const team of captainTeams){const option=document.createElement('option');option.value=team.teamId;option.textContent=team.teamName+' · '+team.seasonName;teamSelect.append(option)}if(requestedTeam&&captainTeams.some((team)=>team.teamId===requestedTeam))teamSelect.value=requestedTeam;teamSelect.disabled=false;renderRoundOptions()}
    function renderRoundOptions(){const team=activeTeam();const rounds=team?.lineupRounds||[];roundSelect.replaceChildren();if(!rounds.length){const option=document.createElement('option');option.value='';option.textContent='No published rounds for this team';roundSelect.append(option);roundSelect.disabled=true;loadButton.disabled=true;submitButton.disabled=true;mobileSubmitButton.disabled=true;return}for(const round of rounds){const option=document.createElement('option');option.value=round.roundId;option.textContent='Round '+round.roundNumber+' · vs '+(round.opponentName||'Opponent')+' · '+dateLabel(round.scheduledOn)+' · Table '+round.tableNumber;roundSelect.append(option)}roundSelect.value=preferredRound(rounds);roundSelect.disabled=false;loadButton.disabled=false;localStorage.setItem('fd.lineupTeamId',team.teamId);localStorage.setItem('fd.lineupRoundId',roundSelect.value)}
    function isSelected(playerId){return selectedSlots.some((slot)=>slot&&slot.playerId===playerId)}function badge(textValue,className){const span=document.createElement('span');span.className='badge '+(className||'');span.textContent=textValue;return span}function playerCard(row){const card=document.createElement('div');card.className='player';card.dataset.blocked=String(!row.eligible);const info=document.createElement('div');const name=document.createElement('div');name.className='player-name';name.textContent=row.display_name;const meta=document.createElement('div');meta.className='meta';meta.append(badge(row.availability_status||'unsure',row.availability_status||'unsure'),badge(row.fargo_rating==null?'Unrated':'Fargo '+row.fargo_rating),badge((row.regular_matches_scheduled||0)+'/7 played'),badge(row.payment_status||'unpaid',(row.payment_status==='paid'||row.payment_status==='waived')?'paid':'blocked'));if(row.eligibility_reason)meta.append(badge(row.eligibility_reason,'blocked'));info.append(name,meta);const add=document.createElement('button');add.type='button';add.className='add';add.textContent=isSelected(row.player_id)?'Selected':'Add';add.disabled=lineupLocked||!row.eligible||isSelected(row.player_id)||selectedSlots.every(Boolean);add.dataset.addPlayer=row.player_id;card.append(info,add);return card}
    function renderCandidates(){const query=searchInput.value.trim().toLowerCase();const visible=availabilityRows.filter((row)=>!query||String(row.display_name).toLowerCase().includes(query));const roster=visible.filter((row)=>row.participation_type==='roster');const subs=visible.filter((row)=>row.participation_type!=='roster');rosterList.replaceChildren();subList.replaceChildren();for(const row of roster)rosterList.append(playerCard(row));for(const row of subs)subList.append(playerCard(row));if(!roster.length){const empty=document.createElement('div');empty.className='empty';empty.textContent=query?'No roster players match.':'No roster players found.';rosterList.append(empty)}if(!subs.length){const empty=document.createElement('div');empty.className='empty';empty.textContent=query?'No substitutes match.':'No outside players have marked themselves available.';subList.append(empty)}rosterCount.textContent=String(roster.length);subCount.textContent=String(subs.length);availabilityCount.textContent=String(availabilityRows.length)}
    function findPlayer(playerId){return availabilityRows.find((row)=>row.player_id===playerId)||null}function addPlayer(playerId){if(lineupLocked)return;const row=findPlayer(playerId);if(!row||!row.eligible||isSelected(playerId))return;const index=selectedSlots.findIndex((slot)=>!slot);if(index<0)return;selectedSlots[index]={playerId:row.player_id,name:row.display_name,type:row.participation_type};renderSlots();renderCandidates()}
    function renderMobileSummary(){mobileSlotsEl.replaceChildren();for(let index=0;index<3;index+=1){const slot=selectedSlots[index];const item=document.createElement('div');item.className='mobile-slot';const label=document.createElement('strong');label.textContent='Slot '+(index+1);const value=document.createElement('span');value.textContent=slot?(slot.forfeit?'Forfeit':slot.name):'Empty';item.append(label,value);mobileSlotsEl.append(item)}const filled=selectedSlots.filter(Boolean).length;mobileSlotCount.textContent=filled+' / 3';mobileSubmitButton.disabled=lineupLocked||filled!==3}
    function renderSlots(){slotsEl.replaceChildren();for(let index=0;index<3;index+=1){const slot=selectedSlots[index];const row=document.createElement('div');row.className='slot';const number=document.createElement('div');number.className='slot-number';number.textContent=String(index+1);const main=document.createElement('div');main.className='slot-main';const strong=document.createElement('strong');const detail=document.createElement('span');if(slot){strong.textContent=slot.forfeit?'Forfeit':slot.name;detail.textContent=slot.forfeit?'No individual loss is created.':(slot.type==='roster'?'Roster player':'Substitute')}else{strong.textContent='Empty slot';detail.textContent='Add a player or mark this slot Forfeit.'}main.append(strong,detail);const actions=document.createElement('div');actions.className='slot-actions';if(!lineupLocked){if(slot){if(index>0){const up=document.createElement('button');up.type='button';up.textContent='↑';up.dataset.moveUp=String(index);actions.append(up)}if(index<2){const down=document.createElement('button');down.type='button';down.textContent='↓';down.dataset.moveDown=String(index);actions.append(down)}const remove=document.createElement('button');remove.type='button';remove.textContent='Remove';remove.className='remove';remove.dataset.removeSlot=String(index);actions.append(remove)}else{const forfeit=document.createElement('button');forfeit.type='button';forfeit.textContent='Forfeit slot';forfeit.className='forfeit';forfeit.dataset.forfeitSlot=String(index);actions.append(forfeit)}}row.append(number,main,actions);slotsEl.append(row)}const filled=selectedSlots.filter(Boolean).length;slotCount.textContent=filled+' / 3';submitButton.disabled=lineupLocked||filled!==3;renderMobileSummary()}
    function moveSlot(from,to){const tmp=selectedSlots[from];selectedSlots[from]=selectedSlots[to];selectedSlots[to]=tmp;renderSlots()}function removeSlot(index){selectedSlots[index]=null;renderSlots();renderCandidates()}function forfeitSlot(index){selectedSlots[index]={forfeit:true,playerId:'',name:'Forfeit',type:'forfeit'};renderSlots();renderCandidates()}
    function renderAvailability(rows){availabilityRows=[...rows].sort((a,b)=>{const rank={available:0,unsure:1,unavailable:2};return(a.participation_type==='roster'?0:1)-(b.participation_type==='roster'?0:1)||(rank[a.availability_status]??1)-(rank[b.availability_status]??1)||String(a.display_name).localeCompare(String(b.display_name))});renderCandidates();renderSlots()}
    function renderLineups(rows){lineupBody.replaceChildren();lineupEmpty.hidden=rows.length>0;lineupCount.textContent=String(rows.length);lineupLocked=rows.some((row)=>row.is_own_team);const opponentVisible=rows.some((row)=>!row.is_own_team);for(const row of rows){const item=document.createElement('div');item.className='committed-row';const team=document.createElement('strong');team.textContent=row.is_own_team?'Your team':'Opponent';const slot=document.createElement('span');slot.textContent='#'+row.slot_number;const player=document.createElement('span');player.textContent=row.display_name||(row.player_id?'Player':'Forfeit');item.append(team,slot,player);lineupBody.append(item)}lockNote.hidden=!lineupLocked;scoreLink.hidden=!(lineupLocked&&opponentVisible);renderSlots();renderCandidates();if(lineupLocked&&opponentVisible)setStatus('Both lineups are locked and revealed. Three matches are ready to score.','ok');else if(lineupLocked)setStatus('Your lineup is locked. Waiting for the opponent captain.','ok')}
    async function loadAvailability(){const body=await api('/api/teams/:teamId/rounds/:roundId/availability',{method:'GET'});renderAvailability(body.availability||[])}async function loadLineups(){const body=await api('/api/teams/:teamId/rounds/:roundId/lineup',{method:'GET'});renderLineups(body.lineups||[])}async function loadPage(){setStatus('Loading lineup...');lineupLocked=false;selectedSlots=[null,null,null];await loadAvailability();await loadLineups();if(!lineupLocked)setStatus('Tap Add to fill three blind-order slots. Available substitutes are listed below your roster.','ok')}
    async function submitLineup(){if(lineupLocked)throw new Error('This lineup is already locked.');if(selectedSlots.filter(Boolean).length!==3)throw new Error('Choose a player or explicit forfeit for all three slots.');const players=selectedSlots.filter((slot)=>slot&&!slot.forfeit).map((slot)=>slot.playerId);if(new Set(players).size!==players.length)throw new Error('Lineup players must be unique');if(!confirm('Lock this lineup? You cannot change the order after submission.'))return;const slots=selectedSlots.map((slot,index)=>({slotNumber:index+1,playerId:slot&&slot.playerId?slot.playerId:''}));setStatus('Locking lineup...');await api('/api/teams/:teamId/rounds/:roundId/lineup',{method:'POST',body:JSON.stringify({slots})});await loadPage()}
    async function bootstrap(){const body=await meApi('/api/me/teams');captainTeams=body.teamManagement?.captain_teams||[];renderTeamOptions();if(!captainTeams.length){setStatus('Create a team or become a captain before submitting a lineup.','error');return}if(!roundSelect.value){setStatus('Your team does not have a published regular-season round yet.','error');return}await loadPage()}async function run(action){try{await action()}catch(error){setStatus(error.message,'error')}}
    statusClose.addEventListener('click',()=>setStatus('Ready.','muted'));form.addEventListener('submit',(event)=>{event.preventDefault();run(loadPage)});teamSelect.addEventListener('change',()=>{renderRoundOptions();if(roundSelect.value)run(loadPage)});roundSelect.addEventListener('change',()=>{if(roundSelect.value){localStorage.setItem('fd.lineupRoundId',roundSelect.value);run(loadPage)}});searchInput.addEventListener('input',renderCandidates);submitButton.addEventListener('click',()=>run(submitLineup));mobileSubmitButton.addEventListener('click',()=>run(submitLineup));refreshButton.addEventListener('click',()=>run(loadPage));document.addEventListener('click',(event)=>{const button=event.target.closest('button');if(!button)return;if(button.dataset.addPlayer)addPlayer(button.dataset.addPlayer);if(button.dataset.removeSlot!=null)removeSlot(Number(button.dataset.removeSlot));if(button.dataset.forfeitSlot!=null)forfeitSlot(Number(button.dataset.forfeitSlot));if(button.dataset.moveUp!=null)moveSlot(Number(button.dataset.moveUp),Number(button.dataset.moveUp)-1);if(button.dataset.moveDown!=null)moveSlot(Number(button.dataset.moveDown),Number(button.dataset.moveDown)+1)});
    renderSlots();if(!accessToken()){setStatus('Sign in with Google to manage a lineup.','error');teamSelect.disabled=true;roundSelect.disabled=true;loadButton.disabled=true;submitButton.disabled=true;mobileSubmitButton.disabled=true;const link=document.createElement('a');link.className='signin';link.href='/profile';link.textContent='Sign in';rosterList.replaceChildren(link)}else run(bootstrap);
  </script>
</body>
</html>`;
}
